# D12 真人测试观察表（观察者填写，不指导）

> 被测人：_________  日期：__________  开始时间：__________  结束时间：__________

## 环境准备（观察者，测试开始前完成）

- [ ] 启动 webapp：`python -c "import sys; sys.path.insert(0, r'<仓库根>'); import plugins.commerce.webapp as w; w.serve()"`
- [ ] 健康检查：`curl -s -o /dev/null -w "%{http_code}" http://127.0.0.1:8765/` 返回 200
- [ ] 测试文件已放桌面：`examples/测试数据-8月26日订单.csv`
- [ ] 计时从被测人拖入文件开始

## 观察记录（每步：独立完成 / 求助 / 卡住）

| 步骤 | 独立完成? | 卡点（犹豫多久、哪里看不懂） | 备注 |
|---|---|---|---|
| 找到并打开工作台 | ☐ 是 ☐ 否 | | |
| 拖入/选择文件 | ☐ 是 ☐ 否 | | |
| 看懂体检问题说明 | ☐ 是 ☐ 否 | | |
| 点击生成报告 | ☐ 是 ☐ 否 | | |
| 看懂报告数字 | ☐ 是 ☐ 否 | | |
| 说出"分享/导出"怎么做 | ☐ 是 ☐ 否 | | |

## 求助记录（≤2 次才可能 PASS）

| # | 问的是什么 | 观察者给的提示 | 属于"文案没说清"还是"操作不会" |
|---|---|---|---|
| 1 | | | |
| 2 | | | |

## 判定

- **PASS 条件**：≤30 分钟；独立完成主旅程；求助 ≤2 次；能说出支付成功率数值与数据日期。
- 结果：☐ PASS　☐ FAIL（卡点：________________________________）

## 口径核对（观察者事后做，不在被测人面前）

- 报告页显示 `pay_success_rate@v2`，数值应为 **87.50%**（订单数口径 7/8）。
- 对账徽标应为「✓ 全部通过」。
- 若数值不是 87.50% 或对账非全过 → 测试环境有问题，本次结果作废，修好再测。

## 记录人签字：_______________

---

## 2026-09-02 第一次真人实测记录（face=C）

- 被测人成功拖入 `测试数据-8月26日订单.csv`，能看懂空白金额提示及影响说明。
- 点击“生成指标报告”后页面仍停留在体检页；重复操作两次均未找到报告或输出位置。
- 台账实查：三次运行均在“建模”阶段显性失败，错误为 dbt 子进程在 Windows
  GBK 默认编码下读取 UTF-8 工程文件触发 `UnicodeDecodeError`；仅有 raw 批次，
  没有生成对应快照或报告。
- 易用性实查：失败信息只通过约 3.2 秒的 toast 显示；没有持久的运行中、成功、
  失败状态，也没有解释“页面报告”和“下载后的 HTML 文件”是两个步骤。
- 本次结果：**FAIL**。不得计为 D12 PASS。

整改后技术侧复验（不替代真人验收）：

- dbt 子进程固定 UTF-8，使用同一失败批次重跑成功。
- 页面运行期间持续显示状态并禁用重复点击；失败时持续显示失败阶段与详情。
- 成功后明确显示“指标报告已生成（当前页面），尚未下载报告文件”。
- 点击“下载报告文件”后显示服务器副本完整路径与 SHA-256。
- 真实浏览器自动化：`tests/test_d12_browser.py` 通过；指标 87.50%，对账全部通过。
- 当前状态：**等待被测人从任务卡起点重新进行 face=C 真人复测**。

## 2026-09-02 第二次真人实测记录（face=C）

- 报告在一分钟内生成并成功下载，核心数字 87.50% 足够醒目，运行速度符合预期。
- 被测人无法从报告标题 `pay_success_rate` 判断这是什么指标，也看不懂数字代表什么。
- `blank_value`、`PASS`、`run_id`、`snapshot_id`、`raw_sha256`、
  `statistical_executor:not_enabled` 等英文技术标识直接占据业务正文，形成理解障碍。
- “LLM 解释未生成”展示了本机端点连接失败异常，被测人无法判断它是否影响报告正确性。
- 本次结果：**FAIL**。主旅程的运行与下载已通过，但 D12 的“知道报告数字是什么意思”未通过。

整改方向：

- 用“支付成功率”作为标题，正文解释“成功支付订单数 ÷ 发起支付订单数”。
- 分子、分母分别经 Semantic API 查询、登记并对账，正文显示确定性的业务白话说明。
- 数据质量说明具体到列、影响和是否影响当前指标。
- LLM 不可用时仍显性记录，但移入折叠技术附录，并说明不影响指标计算和数据核对。
- 所有审计标识、英文方法名和 source 三元组移入折叠技术附录，正文保持业务可读。
- 当前状态：**等待整改后第三次 face=C 真人复测**。

整改后技术侧复验（不替代第三次真人验收）：

- 先以新增用例确认页面模板把 JavaScript 的 `\n` 提前展开，真实浏览器因此整页脚本失效；
  RED 后修复模板转义并加入永久回归用例。
- 发现 Windows 上新旧两个进程同时监听 8765，请求会随机落到旧页面；精确停止重复实例后仅启动最新版。
- 导出报告改为“支付成功率”标题，正文明确 7 笔成功 / 8 笔发起、订单数口径和单日数据边界。
- `amount_total` / `amount_paid` 在正文分别显示为“订单应付金额”/“订单实付金额”；
  `run_id`、哈希、方法名、LLM 状态等仅在主动展开技术附录后显示。
- 默认本机 LLM 端点 `127.0.0.1:20128` 未运行时，技术附录说明“智能文字说明暂不可用，
  不影响指标计算和数据核对”，不再把连接异常放在业务正文。
- `tests/test_d12_browser.py` 真实 Chromium 主旅程 **28/28 PASS**；全量 CI **279 passed**，
  R1-R9 / RT1-RT6 / ALL PASS。
- 当前状态仍为：**第二次真人测试 FAIL，等待第三次 face=C 真人复测**。

第三次复测前新增的用户偏好整改（不计为真人 PASS）：

- 报告页增加“结果概览 / 报告预览”选项卡；预览直接显示最终可下载的 HTML 成品。
- 预览不是下载前置步骤，下载按钮位于选项卡外，用户可跳过预览直接下载。
- “分享报告”文件名、服务器副本路径、文件指纹放在预览框下方；浏览器断言确认这些文字
  不存在于 iframe 内的导出报告正文。
- 预览与下载复用同一份已对账 HTML Blob，避免预览版与下载版不一致或重复导出。
- 当前本机 LLM 端点有连接但可能不返回；face=C 把可选智能说明等待上限缩短为 10 秒，
  超时后显式记录 unavailable 并继续交付，不影响指标和对账。
- 390px 手机宽度实测先 RED（长文件名和路径把页面撑宽），补充强制断行与窄屏间距后 GREEN；
  浏览器永久断言 `scrollWidth <= clientWidth`，避免分享区产生横向滚动。
- 技术复验：D12 浏览器 28/28 PASS；全量 CI **279 passed + R1-R9 / RT1-RT6 / ALL PASS**。

## 2026-09-02 第三次真人实测记录（face=C）

- 被测人在整改后的工作台完成第三次复测，并明确回复“验收通过”。
- 报告可读性整改、可选预览、独立下载及预览下方分享信息未再形成理解或操作卡点。
- 被测人已按验收提示核对业务含义：数据日期 2026-08-26，8 笔发起支付订单中
  7 笔支付成功，支付成功率 87.50%。
- 本次结果：**PASS（face=C）**。
- D12 总项状态：**face=C 真人验收已通过；face=R 真人验收仍待执行**。
