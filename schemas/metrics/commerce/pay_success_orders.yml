metric: pay_success_orders
description: 成功支付订单数（订单去重；status='paid'）
type: count
grain: order
time_column: created_at
filters:
  - { field: status, op: '=', value: 'paid' }
queryable_dimensions: [channel, region]
freshness_contract: { max_delay: PT30M }
owners: [commerce-track]
version: 1
